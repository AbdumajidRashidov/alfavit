{
  "name": "alfavit",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "metrics": "node scripts/metrics.mjs"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
